rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /shopPhotos/{shopId}/{fileName} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.token.email == 'cmkim84@ajunews.com';
    }
  }
}
